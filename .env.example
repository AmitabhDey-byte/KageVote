# Optional. Kiri works with local guide responses when this is absent.
# Never expose this secret through a VITE_ variable or commit it.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash
PORT=8787

# Public configuration; set after a successful Preview/Preprod deployment.
VITE_MIDNIGHT_CONTRACT_ADDRESS=
