# Optional. Configure these in Vercel Project Settings for the Kiri serverless function.
# Kiri works with local guide responses when this is absent.
# Never expose this secret through a VITE_ variable or commit it.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash
PORT=8787

# Do not add a contract address here until deployment succeeds. Record the
# verified network and address in deployment/preprod.json after wallet approval.
